export const token = state => state.token

export const user = state => state.user

export const avatarUrl = state => state.user.avatar_url

export const authorization = state => state.user.authorization
